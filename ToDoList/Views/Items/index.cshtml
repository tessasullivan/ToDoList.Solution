
@{
  Layout = "_Layout";
}
@using ToDoList.Models;
<div class ="grid">
  <div class="middle-of-columns">
    <ul>

    @if (Model.Count == 0)
    {
      <p>You have no items in your to do list!</p>
    }
    @if (Model.Count !=0)
    {
      @foreach (Item item in Model)
      {
      <li><a href='/items/@item.GetId()'>@item.GetDescription() @item.GetDueDate().ToString("MM/dd/yyyy")</a></li>
      }
    }

    </ul>
  </div>
  <div class="middle-of-columns justify-right">
    <form action="/items/deleteAll" method="post">
      <button type="submit" class="button2">Clear All Items</button>
    </form>
  </div>
</div>